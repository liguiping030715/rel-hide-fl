# Ablation experiments

The paper reports component ablations for:

- `k = 1`;
- without APBR;
- without dummy padding.

`no_material_separation/` documents the boundary of the current formal claim. A single-material comparison is not included in the reported formal matrices unless a corresponding script and result manifest are generated.
