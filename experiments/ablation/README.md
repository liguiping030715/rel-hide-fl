# Ablation experiments

The paper reports component ablations for:

- `k = 1`;
- without APBR;
- without dummy padding.

`no_material_separation/` is reserved for future experiments if a single-material comparison is added. It is not part of the current formal result claim unless explicitly generated by the experiment scripts.
